# ========================================
# LICENSE TRACKER - BACKEND CONFIGURATION
# ========================================

# ==================== DATABASE ====================
# MySQL Database Connection
# Format: mysql+pymysql://username:password@host:port/database_name
DATABASE_URL=mysql+pymysql://license_user:your_secure_password@localhost:3306/license_tracker

# ==================== SECURITY ====================
# JWT Secret Key (REQUIRED)
# Generate with: openssl rand -hex 32
# Or online: https://generate-secret.vercel.app/32
# ⚠️ NEVER commit the real secret to Git!
JWT_SECRET_KEY=change-this-to-a-random-secret-key-32-characters-long

# JWT Token Expiration (in minutes)
# 1440 = 24 hours
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# ==================== CORS ====================
# Allowed origins for CORS (comma-separated)
# Development: Use localhost
# Production: Use your actual domain
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# ==================== AI ASSISTANT (Optional) ====================
# OpenAI API Key (Optional - for AI features)
# Get your key from: https://platform.openai.com/api-keys
# Leave blank to disable AI features
OPENAI_API_KEY=

# OpenAI Model (if using AI)
OPENAI_MODEL=gpt-4

# ==================== EMAIL ALERTS (Optional) ====================
# SMTP Configuration for email notifications
# Leave SMTP_USER and SMTP_PASSWORD blank to DISABLE email alerts

# ===== Gmail Setup (Recommended) =====
# 1. Enable 2-Factor Authentication on your Gmail
# 2. Go to: https://myaccount.google.com/apppasswords
# 3. Select "Mail" and "Other (Custom name)"
# 4. Name it: "License Tracker"
# 5. Click "Generate"
# 6. Copy the 16-character password (looks like: xxxx xxxx xxxx xxxx)
# 7. Paste it in SMTP_PASSWORD below (remove spaces)

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=noreply@licensetracker.com

# ===== Other SMTP Providers =====
# Outlook:
# SMTP_HOST=smtp-mail.outlook.com
# SMTP_PORT=587

# Yahoo:
# SMTP_HOST=smtp.mail.yahoo.com
# SMTP_PORT=587

# Custom SMTP:
# SMTP_HOST=smtp.yourdomain.com
# SMTP_PORT=587

# ==================== APPLICATION ====================
# Environment: development, staging, production
ENVIRONMENT=development

# Debug Mode (true/false)
DEBUG=true

# Application Port
PORT=8000

# ==================== LOGGING ====================
# Log Level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Log File Path (optional)
# LOG_FILE=logs/app.log

# ==================== SETUP INSTRUCTIONS ====================
# 1. Copy this file to .env:
#    cp .env.example .env
#
# 2. Update DATABASE_URL with your MySQL credentials
#
# 3. Generate a secure JWT_SECRET_KEY:
#    openssl rand -hex 32
#
# 4. (Optional) To enable email alerts:
#    - Add your Gmail address to SMTP_USER
#    - Add your Gmail App Password to SMTP_PASSWORD
#
# 5. (Optional) To enable AI features:
#    - Add your OpenAI API key to OPENAI_API_KEY
#
# 6. Save and restart the backend server
#
# ==================== SECURITY NOTES ====================
# ⚠️ NEVER commit .env to Git!
# ⚠️ Use strong passwords in production
# ⚠️ Rotate JWT secret regularly
# ⚠️ Use environment-specific values for production
# ⚠️ Gmail App Passwords are safer than regular passwords